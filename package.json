{
  "name": "placement-test",
  "version": "1.0.0",
  "description": "English Placement Test",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step needed, just installing deps'"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  }
}